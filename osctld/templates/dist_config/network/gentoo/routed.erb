# This file is generated by osctld from vpsAdminOS. Do not edit this file, all
# changes will be lost.

config_<%= netif.name %>="
<% netif.active_ip_versions.each do |v| -%>
<%= netif.all_ips(v).map do |addr|
  case v
  when 4
    addr.to_string
  when 6
    "#{addr.to_string} nodad"
  else
    fail "unknown IP version '#{v}'"
  end
end.join("\n") %>
<% end -%>
"

routes_<%= netif.name %>="
<% netif.active_ip_versions.each do |v| -%>
<% if netif.all_ips(v).count > 1 %>default via <%= netif.via[v].host_ip.to_s %> src <%= netif.source_ip(v).to_s %><% end %>
<% end -%>
"
